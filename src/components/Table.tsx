import TabWrapper from "./TabWrapper";
const Table = () => {
  // throw new Error("Test error");
  return (
    <TabWrapper
      activeTab={1}
    >
      <div>Table</div>
    </TabWrapper>
  );
};

export default Table;
